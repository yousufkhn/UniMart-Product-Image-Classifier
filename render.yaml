services:
  - type: web
    name: unimart-vision
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn api.app:app --host 0.0.0.0 --port $PORT"
    plan: free
